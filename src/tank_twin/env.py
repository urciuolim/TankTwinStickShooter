"""Gymnasium environment wrapping the Unity simulator over the socket.

Stub (M0 A1). Migration target for ``tank_env.py`` (gym -> gymnasium port
lands with the env contract; observations are a synthetic RGB grid, CnnPolicy).
No logic yet.
"""
