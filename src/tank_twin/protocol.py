"""TCP-JSON protocol: message framing and (de)serialization.

Stub (M0 A1). Will own strict-JSON encode/decode and the planned message
framing (the live socket is unframed today). RL seam — do not alter the wire
contract without sign-off. No logic yet.
"""
